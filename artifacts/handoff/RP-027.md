# RP-027: 자율 레벨 UI — Level 1/2/3 설정 + 실행 계획 시각화

## Summary
서버에 자율 레벨 설정 API와 메모리 저장소를 추가하고, PlannerUseCase가 자율 레벨에 따라 step 승인 여부를 재계산하도록 수정했다.
대시보드 Overview 탭에 AutonomySelector와 PlanProgress를 추가해 설정 변경과 `plan_progress` WebSocket 시각화를 연결했다.

## Files Changed
| Path | Action | Rationale |
|------|--------|-----------|
| src/yggdrasil/server/core/entities/AutonomyLevel.ts | created | 자율 레벨 타입/설명 정의 |
| src/yggdrasil/server/core/ports/ISettingsRepository.ts | created | 설정 저장소 포트 추가 |
| src/yggdrasil/server/adapters/repositories/InMemorySettingsRepository.ts | created | 기본 Level 1 메모리 저장소 구현 |
| src/yggdrasil/server/adapters/controllers/SettingsController.ts | created | 자율 레벨 GET/PUT API 추가 |
| src/yggdrasil/server/routes/settings.routes.ts | created | settings 라우트 등록 |
| src/yggdrasil/server/core/use-cases/plan/PlannerUseCase.ts | modified | 자율 레벨별 승인 정책 반영 |
| src/yggdrasil/server/di/container.ts | modified | settings 저장소/컨트롤러 조립 |
| src/yggdrasil/server/routes/index.ts | modified | `/api/settings/autonomy` 라우트 마운트 |
| src/yggdrasil/dashboard/components/autonomy-selector.tsx | created | 3단계 자율 레벨 선택 UI 추가 |
| src/yggdrasil/dashboard/components/plan-progress.tsx | created | `plan_progress` 시각화 카드 추가 |
| src/yggdrasil/dashboard/app/page.tsx | modified | Overview 탭에 새 컴포넌트 통합 |
| src/yggdrasil/dashboard/lib/types.ts | modified | `plan_progress` 메시지 타입 추가 |
| src/yggdrasil/server/__tests__/autonomy.test.ts | created | 저장소/API 유효성 테스트 추가 |
| src/yggdrasil/server/__tests__/planner.test.ts | modified | 새 생성자 반영 및 Level 1/3 정책 테스트 추가 |

## Commands Executed
| Command | Result | Notes |
|---------|--------|-------|
| `cd src/yggdrasil && npx tsc --noEmit` | PASS | 타입 에러 없음 |
| `cd src/yggdrasil && npx vitest run` | PASS | 13 files, 65 tests passed |
| `cd src/yggdrasil && node --import tsx server/index.ts` | FAIL | 샌드박스에서 `listen EPERM 0.0.0.0:7777`로 포트 바인딩 불가 |

## Acceptance Criteria Check
- [x] AC-1 — PASS (증거: `server/__tests__/autonomy.test.ts`에서 기본 Level 1 및 GET 응답 확인)
- [x] AC-2 — PASS (증거: `server/__tests__/autonomy.test.ts`에서 PUT set/get 및 invalid level 400 확인)
- [x] AC-3 — PASS (증거: `server/__tests__/planner.test.ts`의 Level 1 강제 승인 테스트)
- [x] AC-4 — PASS (증거: `server/__tests__/planner.test.ts`의 Level 3 위험 작업 승인 테스트)
- [x] AC-5 — PASS (증거: `dashboard/components/autonomy-selector.tsx`에 3개 버튼 및 active 스타일 구현)
- [x] AC-6 — PASS (증거: `dashboard/app/page.tsx`에서 기존 `/ws/status` 메시지로 `plan_progress` 수신, `dashboard/components/plan-progress.tsx` 렌더링)
- [x] AC-7 — PASS (증거: `dashboard/components/plan-progress.tsx`에 progress bar, step 설명, status badge 구현)
- [x] AC-8 — PASS (증거: `npx tsc --noEmit`)
- [x] AC-9 — PASS (증거: `npx vitest run`)
- [ ] AC-10 — FAIL (원인: 샌드박스가 포트 7777 bind를 차단해 실제 `curl http://localhost:7777/api/health` 검증 불가)

## Known Issues / Follow-ups
- 현재 설정 저장소는 메모리 기반이라 서버 재시작 시 자율 레벨이 Level 1로 초기화된다. TP Scope 내 요구사항에 따라 영속화는 추가하지 않았다.
- 서버 실기동 검증은 현재 실행 환경의 포트 바인딩 제한 때문에 수행하지 못했다.

## Recommended Next Actions
1. 포트 바인딩이 가능한 로컬 셸에서 `cd src/yggdrasil && node --import tsx server/index.ts` 후 `curl http://localhost:7777/api/health`를 재검증한다.
2. 자율 레벨을 재시작 간 유지해야 하면 후속 TP에서 file-backed settings repository를 추가한다.

## Context Digest (for Odin)
- Architecture: PlannerUseCase가 설정 저장소를 의존하며 plan 생성 시 승인 정책을 자율 레벨에 맞게 확정한다.
- Key entry points: `/api/settings/autonomy`, `createRouter(container)`, `dashboard/app/page.tsx` Overview 탭.
- Config/env: 추가 환경변수 없음.
- Gotchas: 대시보드 자율 레벨 API는 기존 auth 흐름을 그대로 사용하며, `plan_progress`는 기존 `/ws/status` 채널에 함께 실린다.
